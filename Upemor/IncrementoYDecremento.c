#include <stdio.h>

int main (void){
        int x,y;
        x=10;
        y=10;

        while(x=y){
        x--;
        --y;
        printf("X: %d\tY: %d\n",x,y);
        }

/* En cocnlusion no cambia nada con el preincremento
 * y pre decremento XD*/

return 0;
}
